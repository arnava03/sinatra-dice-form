<!DOCTYPE html>
<html>
  <head>
    <title>Sinatra Dice Form</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
  </head>

  <body>
    <h1><%= @num_dice %>d<%= @sides %></h1>

    <a href="/process_roll?dice=<%= @num_dice %>&sides=<%= @sides %>">Roll again</a> |
    <a href="/">Go back home</a>

    <ol>
      <% @rolls.each do |result| %>
        <li>
          <em>Result:</em> <strong><%= result %></strong>
        </li>
      <% end %>
    </ol>
  </body>
</html>
